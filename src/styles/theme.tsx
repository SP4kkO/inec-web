"use client";
import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#6113A8",
    },
    secondary: {
      main: "#A54AFF",
    },
  },
});

export default Theme;
